---- Cấu hình Local Repo từ Utils ----

🖥️ Cấu hình Máy trạm (Clients)

sudo mkdir /etc/yum.repos.d/old
sudo mv /etc/yum.repos.d/redhat.repo /etc/yum.repos.d/old/

sudo vi /etc/yum.repos.d/rhel9-local.repo
[BaseOS]
name=RHEL9 BaseOS
baseurl=http://10.1.2.5/repos/rhel9/BaseOS
enabled=1
gpgcheck=0

[AppStream]
name=RHEL9 AppStream
baseurl=http://10.1.2.5/repos/rhel9/AppStream
enabled=1
gpgcheck=0


sudo dnf clean all
sudo dnf repolist
dnf install vim -y

###
Đối với control node và ultis node cần disable cả repo từ subscription

sudo subscription-manager repos --disable "*"
sudo dnf config-manager --enable BaseOS AppStream



---- Cấu hình DNS Client ----

### Trên node

dnf install -y bind-utils
nmcli con mod ens160 ipv4.dns "10.1.2.5"
nmcli con up ens160

### Trên utils và control

dnf install -y bind-utils
nmcli con mod ens192 ipv4.dns "10.1.2.5"
nmcli con up ens192


---- Cấu hình Time Client ----

# --- Cài chrony ---
dnf install -y chrony

# --- Xoá dòng server cũ, comment các pool có sẵn ---
sed -i '/^server /d' /etc/chrony.conf
sed -i 's/^pool /#pool /' /etc/chrony.conf

# --- Thêm NTP server nội bộ (utils) ---
echo "server 10.1.2.5 iburst" >> /etc/chrony.conf

# --- Khởi động dịch vụ ---
systemctl enable --now chronyd

# --- Kiểm tra đồng bộ ---
chronyc sources -v
chronyc tracking



